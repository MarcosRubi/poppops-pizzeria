<section class="hero d-flex align-center py-3">
    <div class="container d-flex-lg align-center justify-between">
        <div class="hero__header">
            <span>Eat Pizza Everyday</span>
            <h1>SHARE YOUR LOVE FOR PIZZA</h1>
            <button class="btn btn-primary rounded d-flex align-center">
                <i class="fa fa-phone-alt"></i>
                <span class="text-lg">Contact Us</span>
            </button>
        </div>
        <div class="hero__img d-flex p-relative">
            <img
                src="/images/pizza-hero.webp"
                alt="pizza"
            />
            <div class="price rounded bg-white d-flex justify-center align-center flex-column text-xl p-absolute">
                <span class="d-block">Only</span>
                <span class="d-block bold">$14.95</span>
            </div>
        </div>
    </div>
</section>

<style lang="scss">
    .hero {
        min-height: calc(100vh - 12rem);
        background-color: var(--black);
        color: var(--white);
        &__header {
            h1 {
                letter-spacing: 0.15rem;
                margin: 1rem 0;
            }
            .btn span {
                padding-left: 0.5rem;
            }
        }
        &__img {
            img {
                margin-top: 3rem;
            }
            .price {
                border-radius: 50%;
                width: 7rem;
                height: 7rem;
                color: var(--black);
                top: 3rem;
            }
        }
    }

    @media (min-width: 768px) {
        .hero {
            &__header {
                > span {
                    font-size: 2rem;
                }
            }
        }
    }
    @media (min-width: 1024px) {
        .hero {
            &__img {
                flex-basis: 100%;
                justify-content: flex-end;
            }
        }
    }
</style>
